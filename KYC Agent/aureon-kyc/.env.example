# Copy to .env and fill in locally. Never commit .env — see the root
# .gitignore. This file was previously committed with a live key in it;
# it is intentionally empty now.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=
ANTHROPIC_MAX_TOKENS=
PORT=
